import abstractClass from "../../Utils/abstraction.js";

class abstractAuth0Data extends abstractClass {
  constructor() {
    super(abstractAuth0Data);
  }

  getCode() {
    this.abstractMethodFactory(this.getCode);
  }

  getClientId() {
    this.abstractMethodFactory(this.getClientId);
  }

  getClientSecret() {
    this.abstractMethodFactory(this.getClientSecret);
  }
}

export default abstractAuth0Data;
